spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/atas_test
    username: atas
    password: ataspass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false

# Test configuration
atas:
  storage:
    bucket: test-bucket
    region: us-east-1
    video-folder: test-videos
    screenshot-folder: test-screenshots

# Logging for tests
logging:
  level:
    com.atas: INFO
    org.springframework.test: INFO
    org.hibernate.SQL: DEBUG